# Copilot Skills

Store reusable Copilot skills as folder-based packages in this directory.

Use skills for detailed procedural workflows that should only load when relevant.

Examples:

- `ba-workspace/SKILL.md` for lightweight input/output workspace setup and usage
- `git-operations/SKILL.md` for safe, repeatable repository Git workflows
