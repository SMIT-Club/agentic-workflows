# Team Instructions

Store shared instruction files as `*.instructions.md` in this folder.

Use these for domain language, glossary rules, and output standards that should apply broadly across BA work.

Keep `applyTo` patterns specific to avoid loading irrelevant instructions into context.

Examples:

- `git-operations.instructions.md` for repository Git safety and workflow rules
